# networks:
#   default:
#     name: presenton-network
#     driver: bridge

# volumes:
#   app_data: 

services:
  production:
    image: presenton:chinese
    ports:
      # You can replace 5000 with any other port number of your choice to run Presenton on a different port number.
      - "5000:80"
    volumes:
      - ./app_data:/app_data
    environment:
      - DATABASE_URL=postgresql://presenton:123456@postgres:5432/presenton
      - CAN_CHANGE_KEYS=${CAN_CHANGE_KEYS}
      - LLM=${LLM}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - GOOGLE_MODEL=${GOOGLE_MODEL}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
      - OLLAMA_URL=${OLLAMA_URL}
      - OLLAMA_MODEL=${OLLAMA_MODEL}
      - CUSTOM_LLM_URL=${CUSTOM_LLM_URL}
      - CUSTOM_LLM_API_KEY=${CUSTOM_LLM_API_KEY}
      - CUSTOM_MODEL=${CUSTOM_MODEL}
      - PEXELS_API_KEY=${PEXELS_API_KEY}
      - EXTENDED_REASONING=${EXTENDED_REASONING}
      - TOOL_CALLS=${TOOL_CALLS}
      - DISABLE_THINKING=${DISABLE_THINKING}
      - WEB_GROUNDING=${WEB_GROUNDING}
      - DISABLE_ANONYMOUS_TRACKING=${DISABLE_ANONYMOUS_TRACKING}
      - COMFYUI_URL=${COMFYUI_URL}
      - COMFYUI_WORKFLOW=${COMFYUI_WORKFLOW}
      - LOG_LEVEL=${LOG_LEVEL}
    depends_on:
      - postgres
  postgres:
    image: postgres:latest
    container_name: presenton-postgres
    environment:
      POSTGRES_USER: presenton
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: presenton
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U presenton -d presenton"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    # networks:
    #   - presenton-network
  # production-gpu:
  #   # image: ghcr.io/presenton/presenton:latest
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   ports:
  #     # You can replace 5000 with any other port number of your choice to run Presenton on a different port number.
  #     - "5000:80"
  #   volumes:
  #     - ./app_data:/app_data
  #   environment:
  #     - CAN_CHANGE_KEYS=${CAN_CHANGE_KEYS}
  #     - LLM=${LLM}
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - OPENAI_MODEL=${OPENAI_MODEL}
  #     - GOOGLE_API_KEY=${GOOGLE_API_KEY}
  #     - GOOGLE_MODEL=${GOOGLE_MODEL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
  #     - OLLAMA_URL=${OLLAMA_URL}
  #     - OLLAMA_MODEL=${OLLAMA_MODEL}
  #     - CUSTOM_LLM_URL=${CUSTOM_LLM_URL}
  #     - CUSTOM_LLM_API_KEY=${CUSTOM_LLM_API_KEY}
  #     - CUSTOM_MODEL=${CUSTOM_MODEL}
  #     - PEXELS_API_KEY=${PEXELS_API_KEY}
  #     - EXTENDED_REASONING=${EXTENDED_REASONING}
  #     - TOOL_CALLS=${TOOL_CALLS}
  #     - DISABLE_THINKING=${DISABLE_THINKING}
  #     - WEB_GROUNDING=${WEB_GROUNDING}
  #     - DATABASE_URL=${DATABASE_URL}
  #     - DISABLE_ANONYMOUS_TRACKING=${DISABLE_ANONYMOUS_TRACKING}
  #     - COMFYUI_URL=${COMFYUI_URL}
  #     - COMFYUI_WORKFLOW=${COMFYUI_WORKFLOW}
  
  # development:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   ports:
  #     - "5000:80"
  #   volumes:
  #     - .:/app
  #     - ./app_data:/app_data
  #   environment:
  #     - CAN_CHANGE_KEYS=${CAN_CHANGE_KEYS}
  #     - LLM=${LLM}
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - OPENAI_MODEL=${OPENAI_MODEL}
  #     - GOOGLE_API_KEY=${GOOGLE_API_KEY}
  #     - GOOGLE_MODEL=${GOOGLE_MODEL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
  #     - OLLAMA_URL=${OLLAMA_URL}
  #     - OLLAMA_MODEL=${OLLAMA_MODEL}
  #     - CUSTOM_LLM_URL=${CUSTOM_LLM_URL}
  #     - CUSTOM_LLM_API_KEY=${CUSTOM_LLM_API_KEY}
  #     - CUSTOM_MODEL=${CUSTOM_MODEL}
  #     - PEXELS_API_KEY=${PEXELS_API_KEY}
  #     - EXTENDED_REASONING=${EXTENDED_REASONING}
  #     - TOOL_CALLS=${TOOL_CALLS}
  #     - DISABLE_THINKING=${DISABLE_THINKING}
  #     - WEB_GROUNDING=${WEB_GROUNDING}
  #     - DATABASE_URL=${DATABASE_URL}
  #     - DISABLE_ANONYMOUS_TRACKING=${DISABLE_ANONYMOUS_TRACKING}
  #     - COMFYUI_URL=${COMFYUI_URL}
  #     - COMFYUI_WORKFLOW=${COMFYUI_WORKFLOW}

  # development-gpu:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   ports:
  #     - "5000:80"
  #   volumes:
  #     - .:/app
  #     - ./app_data:/app_data
  #   environment:
  #     - CAN_CHANGE_KEYS=${CAN_CHANGE_KEYS}
  #     - LLM=${LLM}
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - OPENAI_MODEL=${OPENAI_MODEL}
  #     - GOOGLE_API_KEY=${GOOGLE_API_KEY}
  #     - GOOGLE_MODEL=${GOOGLE_MODEL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
  #     - OLLAMA_URL=${OLLAMA_URL}
  #     - OLLAMA_MODEL=${OLLAMA_MODEL}
  #     - CUSTOM_LLM_URL=${CUSTOM_LLM_URL}
  #     - CUSTOM_LLM_API_KEY=${CUSTOM_LLM_API_KEY}
  #     - CUSTOM_MODEL=${CUSTOM_MODEL}
  #     - PEXELS_API_KEY=${PEXELS_API_KEY}
  #     - EXTENDED_REASONING=${EXTENDED_REASONING}
  #     - TOOL_CALLS=${TOOL_CALLS}
  #     - DISABLE_THINKING=${DISABLE_THINKING}
  #     - WEB_GROUNDING=${WEB_GROUNDING}
  #     - DATABASE_URL=${DATABASE_URL}
  #     - DISABLE_ANONYMOUS_TRACKING=${DISABLE_ANONYMOUS_TRACKING}
  #     - COMFYUI_URL=${COMFYUI_URL}
  #     - COMFYUI_WORKFLOW=${COMFYUI_WORKFLOW}